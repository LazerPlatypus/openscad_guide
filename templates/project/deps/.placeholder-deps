# Dependencies Directory

This directory is for git submodules and external dependencies.

To add a dependency:
```bash
git submodule add <repository-url> deps/<name>
```

Dependencies should be excluded from documentation in .openscad_docsgen_rc
